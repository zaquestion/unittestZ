from distutils.core import setup
setup(
  name = 'unittestZ',
  packages = ['unittestZ'], # this must be the same as the name above
  version = '0.1',
  description = 'Drop in replacement for unittest2/nose test assertion library',
  author = 'Zaq? Wiedmann',
  author_email = 'zaquestion@gmail.com',
  url = 'https://github.com/zaquestion/unittestZ', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/tarball/0.1', # I'll explain this in a second
  keywords = ['testing', 'assert', 'assertion', 'unittest2', 'nose'], # arbitrary keywords
  classifiers = [],
)
`
